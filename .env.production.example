# Frontend Environment Variables (Production)
# Set these in your hosting provider's environment variables (Vercel, Cloudflare Pages, etc.)
# DO NOT commit actual values to git

# Frontend -> API
VITE_API_URL=

# Supabase (Frontend uses ANON key only - safe for browser)
# WARNING: Never use SERVICE_ROLE_KEY in frontend - it's a secret!
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=