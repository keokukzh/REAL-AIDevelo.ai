# Frontend Environment Variables (Development)
# Copy this file to .env.local and fill in your values
# .env.local is NOT committed to git

# Frontend -> API
VITE_API_URL=

# Supabase (Frontend uses ANON key only - safe for browser)
# WARNING: Never use SERVICE_ROLE_KEY in frontend - it's a secret!
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=